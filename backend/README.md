# Backend Services

This directory holds operator-facing services for the stablecoin stack.

- `mint-burn-service`: request and execution flow for issuance and redemption
- `compliance-service`: blacklist and audit automation
- `indexer`: event ingestion and webhook dispatch

Each service currently exposes health endpoints only.

