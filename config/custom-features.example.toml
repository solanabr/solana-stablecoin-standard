# Custom stablecoin feature configuration
# Use with: sss-token init --preset custom --config config/custom-features.example.toml

freeze_authority = true
permanent_delegate = true
transfer_hook = false
confidential_transfers = false
